publish:
    name: Publicar na PSGallery
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Publicar Modulo
        shell: pwsh
        env:
          PSG_KEY: ${{ secrets.PSGALLERY_API_KEY }}
        run: |
          $ErrorActionPreference = "Stop"
          
          # Força o uso de protocolos seguros (Resolve falhas de conexão)
          [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12

          # Atualiza as ferramentas de base (Passo crucial para o Ubuntu)
          Write-Host "🔄 Atualizando ferramentas de gerenciamento de pacotes..."
          Install-Module -Name PowerShellGet -Force -SkipPublisherCheck -Scope CurrentUser
          Install-Module -Name PackageManagement -Force -SkipPublisherCheck -Scope CurrentUser
          
          # Importa o PackageManagement para habilitar o provedor NuGet
          Import-Module PackageManagement
          Install-PackageProvider -Name NuGet -MinimumVersion 2.8.5.201 -Force -Confirm:$false

          # Configura e publica
          Set-PSRepository -Name PSGallery -InstallationPolicy Trusted
          $modulePath = Resolve-Path "./src/PoshAutomator"
          
          Write-Host "🚀 Publicando versão oficial na PowerShell Gallery..."
          Publish-Module -Path $modulePath -NuGetApiKey $env:PSG_KEY -Verbose -Force